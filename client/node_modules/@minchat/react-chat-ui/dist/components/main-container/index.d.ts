import React from 'react';
import "../../index.css";
export interface Props {
    style?: React.CSSProperties | undefined;
    children?: React.ReactNode;
}
export default function MainContainer({ children, style }: Props): React.JSX.Element;
