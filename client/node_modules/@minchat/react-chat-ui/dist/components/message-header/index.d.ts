import React from 'react';
export declare type Props = {
    onBack?: () => void;
    children?: React.ReactNode;
    showBack?: boolean;
    mobileView?: boolean;
    lastActive?: Date;
};
export default function MessageHeader({ onBack, children, showBack, mobileView, lastActive }: Props): React.JSX.Element;
