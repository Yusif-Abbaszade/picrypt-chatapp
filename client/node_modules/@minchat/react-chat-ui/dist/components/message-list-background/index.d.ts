import React from 'react';
declare type Props = {
    mobileView?: boolean;
    roundedCorners?: boolean;
};
export default function MessageListBackground({ mobileView, roundedCorners }: Props): React.JSX.Element;
export {};
