import React from 'react';
import MessageType from '../../types/MessageType';
export declare type Props = {
    title: string;
    lastMessage?: MessageType;
    unread?: boolean;
    avatar?: string;
    onClick: () => void;
    selected?: boolean;
    /**
     * the current user on the chat ui
     */
    currentUserId?: string;
};
export default function Conversation({ title, lastMessage, onClick, avatar, selected, currentUserId, unread }: Props): React.JSX.Element;
