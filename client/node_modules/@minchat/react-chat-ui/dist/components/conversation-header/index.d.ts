import React from 'react';
export declare type Props = {
    showHeader?: boolean;
    loading?: boolean;
};
export default function ConversationHeader({ loading, showHeader }: Props): React.JSX.Element;
