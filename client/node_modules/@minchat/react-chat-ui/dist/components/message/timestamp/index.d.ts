import React from 'react';
declare type Props = {
    loading?: boolean;
    date?: Date;
    seen?: boolean;
    showSeen?: boolean;
    color?: string;
    loaderColor?: string;
    checkmarkColor?: string;
};
export declare const Content: any;
export default function Timestamp({ loading, date, showSeen, seen, color, loaderColor, checkmarkColor }: Props): React.JSX.Element;
export {};
