import React from 'react';
import { MediaType } from '../../../types/MessageType';
interface Props extends MediaType {
    last?: boolean;
    single?: boolean;
    messageType: "incoming" | "outgoing";
}
export default function MediaContent({ type, url, size, last, single, messageType }: Props): React.JSX.Element;
export {};
