import React from 'react';
import User from '../../types/UserType';
import { MediaType } from '../../types/MessageType';
export declare type Props = {
    created_at?: Date;
    seen?: boolean;
    text?: string;
    media?: MediaType;
    loading?: boolean;
    type?: "incoming" | "outgoing";
    user?: User;
    showAvatar?: boolean;
    showHeader?: boolean;
    last?: boolean;
    single?: boolean;
    clusterFirstMessage?: boolean;
    clusterLastMessage?: boolean;
};
export default function Message({ text, media, created_at, seen, loading, type, user, showAvatar, showHeader, last, single, clusterFirstMessage, clusterLastMessage }: Props): React.JSX.Element;
