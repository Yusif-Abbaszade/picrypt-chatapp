import React from 'react';
import { Props } from '..';
export default function IncomingMessage({ text, media, user, showAvatar, showHeader, last, single, created_at, }: Omit<Props, "type" | "clusterFirstMessage" | "clusterLastMessage" | "seen">): React.JSX.Element;
